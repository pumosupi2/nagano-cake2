<h1>管理者トップページ(注文履歴一覧)</h1>

<h2>注文履歴一覧</h2>
<table>
  <tr>
    <th>購入日時</th>
    <th>購入者</th>
    <th>注文個数</th>
  </tr>
 <% @orders.each do |order| %>
  <tr>
    <td><%= link_to order.created_at.strftime("%Y-%m-%d %H:%M:%S"), admin_order_path(order.id) %></td>
    
    <td><%= order.customer.full_name %></td>
    <td><%= order.total_amount %></td>

  </tr>
  <% end %>
</table>
<%= paginate @orders %>
