<div class='container px-5 px-sm-0'>
  <div class='row'>
    <h3>管理者側の商品詳細</h3>
    <div class='row'>
      <div class='col'>
        <%= image_tag @item.get_image(400,300) %></br>
      </div>
      <div class='col'>
        商品名
        <%= @item.name %></br>
        商品説明
        <%= @item.introduction %></br>
        ジャンル
        <%= @item.genre.name %></br>
        税込価格</br>
        （税抜価格）
        <%= ((@item.price * 1.1).round(2)).ceil.to_s(:delimited) %>
        (<%= @item.price.to_s(:delimited) %>)円</br>
        販売ステータス
        <% if @item.is_active %>
          販売中
        <% else %>
          販売停止中
        <% end %></br>
        <%= link_to '編集する', edit_admin_item_path, class: "btn btn-warning" %>
      </div>
    </div>
  </div>
</div>